pre-commit:
  jobs:
    - name: lint and fix
      run: pnpm run lint
pre-push:
  jobs:
    - name: Test unit
      run: pnpm run test

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: pnpm commitlint --edit $1
