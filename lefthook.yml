pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm run lint

commit-msg:
  commands:
    conventional-commit:
      run: pnpm commitlint --edit $1

pre-push:
  commands:
test:
      run: pnpm run lint
